##directories and graph build

directories:
  pockets: '~/dataset_graph/data'
  pos_list: '~/dataset_graph/TOUGH-M1/TOUGH-M1_positive.list'
  neg_list: '~/dataset_graph/TOUGH-M1/TOUGH-M1_negative.list'
  cluster_map: '~/graphpocket/cluster_map.pkl'
  result_dir: '~/graphpocket/results'

graph:
  threshold_k: 10
  algorithm: 'bruteforce-blas'

##model config

model:
  input_scalar_size: 24
  output_scalar_size: 128
  edge_feat_size: 1
  vector_size: 16
  n_convs: 4
  dropout: 0.3
  message_norm: 24
  vector_gating: true
  xavier_init: true

loss:
  margin: 1.2

##train config and hyperparameters

train:
  n_epochs: 100
  batch_size: 64
  n_workers: 6

  optimizer:
    type: 'Adam'
    lr: 0.005
    weight_decay: 0.0001
  
  scheduler:
    type: "StepLR"
    params:
      step_size: 25
      gamma: 0.2
      
#wandb sweep args: dropout, margin, lr, weight_decay, step_size, gamma, n_convs
